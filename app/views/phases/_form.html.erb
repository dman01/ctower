<%= form_for(@phase) do |f| %>
  <% if @phase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phase.errors.count, "error") %> prohibited this phase from being saved:</h2>

      <ul>
      <% @phase.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :sortOrder %><br />
    <%= f.number_field :sortOrder %>
  </div>
  <div class="field">
    <%= f.label :portfolioProcess_id %><br />
    <%= f.number_field :portfolioProcess_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
